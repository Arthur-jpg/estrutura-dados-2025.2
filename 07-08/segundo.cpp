#include <iostream> // lib for input/output

using namespace std; // para não ficar colocando std:: antes de cada comando

int main() {
    int x, y;
    cin >> x;
    cin >> y;

    cout << "X = " << x + y << endl; 


    return 0;
}