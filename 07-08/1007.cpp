#include <iostream>
#include <stdio.h>

using namespace std;

int main() {
    int a,b,c,d;
    cin >> a >> b >> c >> d;
    int diferenca = ((a*b)-(c*d));
    printf("DIFERENCA = %d\n", diferenca);
    return 0;
}