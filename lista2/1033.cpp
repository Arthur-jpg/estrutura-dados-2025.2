#include <iostream>
#include <stdio.h>

using namespace std;


int main() {

    int n,b;

    cin >> n >> b;

    if (n <= 0 || b <= 0) {
        return 0;
    }

    int fib;

    while (cin >> fib) {
        
    }
    
    return 0;
}